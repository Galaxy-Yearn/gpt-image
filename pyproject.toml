[project]
name = "codex-gpt-image-skill"
version = "0.1.0"
description = "Codex skill for generating images with GPT image models through a configurable OpenAI-compatible Images API."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
